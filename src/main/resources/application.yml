server:
    port: 7979

spring:
    cloud:
      config:
          server:
            git:
              uri: https://github.com/paulgstevenson/config

info:
  component: Config Samples
  endpoints:
    restart:
    enabled: true
    logging:
      levels:
        org.springframework.boot.env.PropertySourcesLoader: TRACE
        org.springframework.web: DEBUG